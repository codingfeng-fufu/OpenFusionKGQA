# GraphRAG成本优化项目依赖包
# Python 3.8+

# ============ 核心依赖 ============
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
networkx>=2.6.0

# ============ NLP工具 ============
# 中文分词和关键词提取
jieba>=0.42.1
# 英文NER（可选）
# spacy>=3.0.0

# ============ 文本相似度 ============
sentence-transformers>=2.2.0

# ============ 图数据库（可选）============
# neo4j>=5.0.0

# ============ LLM API ============
openai>=1.0.0
# anthropic>=0.3.0  # 如果使用Claude API

# ============ 可视化 ============
matplotlib>=3.5.0
seaborn>=0.11.0
# plotly>=5.0.0  # 交互式可视化（可选）

# ============ 测试 ============
pytest>=7.0.0
pytest-cov>=3.0.0

# ============ 工具 ============
tqdm>=4.62.0  # 进度条
pyyaml>=6.0  # 配置文件
python-dotenv>=0.19.0  # 环境变量

# ============ Jupyter（可选）============
jupyter>=1.0.0
ipykernel>=6.0.0

# ============ 数据处理 ============
# 如果需要处理PDF
# pypdf2>=2.0.0
# 如果需要处理Word文档
# python-docx>=0.8.11

# ============ 数据集（可选）============
# datasets>=2.0.0  # Hugging Face datasets

# ============ 评估指标 ============
rouge-score>=0.1.2  # ROUGE评分
# bert-score>=0.3.0  # BERTScore（可选）

# ============ 其他工具 ============
requests>=2.28.0
beautifulsoup4>=4.11.0  # 如果需要网页抓取

# ============ 安装说明 ============
# 
# 基础安装:
#   pip install -r requirements.txt
#
# 如果遇到网络问题，使用国内源:
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 只安装核心依赖（不含可选包）:
#   pip install numpy pandas scikit-learn networkx jieba sentence-transformers openai matplotlib pytest pyyaml tqdm
#
# 成员B最小依赖（只做二部图）:
#   pip install numpy pandas scikit-learn jieba matplotlib pytest
